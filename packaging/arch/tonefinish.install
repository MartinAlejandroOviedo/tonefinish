post_install() {
  if [ ! -d /usr/lib/tonefinish/.venv ]; then
    python -m venv /usr/lib/tonefinish/.venv
  fi
  /usr/lib/tonefinish/.venv/bin/pip install -r /usr/lib/tonefinish/requirements.txt
}

post_upgrade() {
  post_install
}
